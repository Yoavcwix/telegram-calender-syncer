# Telegram Bot (required for the webhook)
# Create a bot via @BotFather, then paste the token here.
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Google Calendar (required for creating events)
# Use your primary calendar email, or a calendar ID from Google Calendar settings.
# Primary: your@gmail.com
# Other: copy "Integrate calendar" → Calendar ID from calendar settings.
GOOGLE_CALENDAR_ID=primary

# Optional: timezone for new events (default: Asia/Jerusalem)
# Examples: America/New_York, Europe/London, UTC
# TIMEZONE=Asia/Jerusalem
